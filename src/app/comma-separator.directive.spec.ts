import { CommaSeparatorDirective } from './comma-separator.directive';

describe('CommaSeparatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaSeparatorDirective();
    expect(directive).toBeTruthy();
  });
});
